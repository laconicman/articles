# Сверка фактов и обзор ландшафта: статья о Express API Яндекс Доставки

Служебная записка к посту «…» (слаг `yandex-delivery-express-api`). На сайт не публикуется:
папка `notes/` исключена в `_config.yml`.

**Дата сверки документации — 2026-08-24.** Всё, что ниже помечено «док», взято в этот день с
`yandex.com/support/delivery-profile/ru/api/express/…` (Diplodoc Platform v5.55.2). Всё, что помечено
«провод», — наблюдения из живых вызовов, записанные в DocC-статьях пакета `YandexDeliveryExpress`
(`WorkingWithYandex`, `TechDebt`), с их собственными датами. Тестовый аккаунт, не продовый.

## 1. Машиночитаемого документа нет

Справочник отрендерен **из** OpenAPI-документа: страницы вида
`/api/express/openapi/IntegrationV2ClaimsCreate` показывают схемы, типы, `Pattern`, `Enum`, коды ответов.
На странице нет ни одной ссылки, ведущей на `.json`/`.yaml`, нет слов «скачать» или «спецификация»
(проверено программно: выборка всех `<a href>` по маске `\.(json|ya?ml)|download|spec` — пусто).
Заголовка `Last-Modified` страницы не отдают (`cache-control: no-store`).

Следствие: сгенерировать клиент по-прежнему не из чего. Документ у вендора есть — наружу отдан рендер.

**Источник ключевого утверждения, отличный от моей проверки:** автор сообщает, что его компания
направила Яндексу официальный запрос и получила официальный ответ — **спецификации нет**. Это
единственный факт в статье, который я не могу подтвердить самостоятельно; он приведён как слова автора
и снимает вопрос «может, плохо искали». Если переписка сохранилась, ссылка или дата в тексте усилили бы
это место.

## 2. История изменений остановилась в 2023 году

`releases.md` — таблица из трёх строк, все три датированы **17.07.2023** (добавление
`offers/calculate`, параметр `offer_payload` в `claims/create`, `expected_visit_interval` в ответе
`claims/info`).

При этом в оглавлении (`llms.txt`) сегодня перечислены методы, которых в той таблице нет вовсе:
редактирование заявки до и после подтверждения (`IntegrationV2ClaimsEdit`, `ClaimsApplyChangesRequest`,
`ClaimsApplyChangesResult`), пропуск точки (`IntegrationV2ClaimsReturn`), поиск и журнал изменений,
код подтверждения, подтверждение доставки, готовность точки к выдаче и три метода обмена
**с роботом** (`IntegrationV1RobotHandoverInitialize/Info/Confirm`).

То есть API за три года заметно вырос, а журнал изменений об этом молчит. Узнать, когда что
изменилось, читателю неоткуда.

## 3. Опечатка `droppof_point` — в примерах запроса, а не только в ответе

Док, `IntegrationV2ClaimsCreate`, пример тела запроса (встречается в файле четырежды):

```json
"pickup_point": 1,
"dropoff_point": 2,
"droppof_point": 0,
```

Описание поля в схеме — целиком, дословно: **«deprecated, use dropoff_point»**. Ни даты, ни срока
удаления; сам флаг `deprecated` OpenAPI при этом не выставлен — это проза в `description`.
В примере два поля с одинаковым смыслом имеют **разные значения** (`2` и `0`).

Провод (2026-08-12): в ответе каждый товар несёт оба поля с одинаковым значением
(`"dropoff_point": 26389626087876, "droppof_point": 26389626087876`).

## 4. Два словаря кодов ошибок — и они противоречат друг другу внутри документации

`errors.md` — «Справочник ошибок» на **11 строк** для API из тридцати с лишним методов. В колонке
«Сообщение» вперемешку символьные коды и человеческие фразы:

| Код | Сообщение (дословно) |
|---|---|
| 401 | `unauthorized` |
| 500 | `Internal server error` |
| 409 | `inappropriate_status` |
| 400 | `Parse error` |
| 400 | `too_many_loaders` — «Максимальное количество грузчиков — 2.» |
| 400 | `delay_too_long` — «Максимальное количество дней для поля `due` — 3.» |

А на странице `IntegrationV2OfferCalculate` ответ **409** документирован закрытым перечислением из
**23** значений, среди которых `estimating.too_many_loaders`, `estimating.warning.too_heavy_item`,
`estimating.route_too_short`, `estimating.swapped_coordinates`.

Итого одна и та же ошибка «слишком много грузчиков» описана в справочнике как `too_many_loaders` / 400,
а на странице метода — как `estimating.too_many_loaders` / 409. Провод (2026-08-12) подтверждает
второй вариант: `409 {"code":"estimating.too_many_loaders","message":"В выбранном кузове не получится
заказать столько грузчиков"}`.

Провод же показывает и третий регистр — числовой код-строку с внутренностями парсера:

```json
{"code":"400","message":"Parse error at pos 763, path 'route_points': incorrect size, must be 2 (limit) <= 1 (value)"}
{"code":"400","message":"Value of query 'claim_id': incorrect size, must be 32 (limit) <= 3 (value)"}
```

## 5. Три дня против пяти суток

Док, `errors.md`: «`delay_too_long` — Максимальное количество дней для поля `due` — 3».
Док, сущность `Due` на странице `claims/create`: «на 30-240 минут … для тарифа `express`; **на пять
суток** от текущего момента – для тарифа `cargo`».

Обе страницы актуальны на 2026-08-24.

## 6. Коды ответов: что объявлено у нас и что у них

Наш рукописный `openapi.yaml` (2 028 строк, 6 операций) против справочника Яндекса:

| Операция | Наш документ | Справочник Яндекса |
|---|---|---|
| `offers/calculate` | 200 400 401 409 429 500 | 200 400 409 429 |
| `claims/create` | 200 400 401 429 500 | 200 400 **403** 429 |
| `claims/info` | 200 400 401 404 429 500 | 200 400 404 429 |
| `claims/accept` | 200 400 401 409 429 500 | 200 404 409 429 |
| `claims/cancel-info` | 200 400 401 404 409 429 500 | 200 400 404 |
| `claims/cancel` | 200 400 401 409 429 500 | 200 400 404 409 |

**401 не объявлен ни у одной операции**, хотя `errors.md` его документирует, а провод (2026-08-12)
вернул именно `401 {"code":"unauthorized","message":"Access denied"}`. То же с 500. Зато `403` на
`claims/create` есть у них и нет у нас — это дырка в нашем документе, и её надо признать.

Отдельно: 409 на `offers/calculate` мы добавили себе руками 2026-08-12, когда живой вызов вернул
`.undocumented` (TD-17). Сегодня он есть и у Яндекса. Когда он там появился — установить нельзя,
см. пункт 2.

## 7. Время

Док объявляет `string<date-time>`. Примеры в самой документации приходят в двух видах:
`"2020-01-01T07:00:00+00:00"` и `"2023-07-17T08:02:26.607358+00:00"`.

Провод (2026-08-12, TD-16): один ответ `offers/calculate` — 21 отметка с шестизначной дробной частью
и 4 без неё, включая один интервал, у которого `from` с дробью, а `to` без:

```json
"pickup_interval": {
  "from": "2026-08-12T17:12:40.051944+00:00",
  "to":   "2026-08-12T18:15:00+00:00"
}
```

**Важная оговорка для честности текста:** обе формы валидны по RFC 3339 — дробная часть там
опциональна. Претензия не в нарушении стандарта, а в том, что формат меняется внутри одного объекта,
и потому читающая сторона обязана быть либеральной, а пишущая — угадывать.

## 8. Деньги

Док, `Pattern` для `total_price` и `total_price_with_vat`: `^-?[0-9]{1,14}(\.[0-9]{0,4})?$`.
Этот шаблон допускает четыре знака после запятой у денег и — из-за `{0,4}` — строку с точкой и
пустой дробной частью (`"1449."`).

Провод: `"total_price":"1449"` и `"total_price_with_vat":"1767.78"` в одном объекте.

## 9. Вебхук склеивает URL конкатенацией — и это задокументировано

Док, `claims/create`, описание `callback_properties`, дословно:

> Важно: параметры добавляются конкатенацией к callback_url, то есть url вида 'https://example.com'
> превратится в невалидный 'https://example.comupdated_ts=...&claim_id=...'.

То есть дефект описан вместо того, чтобы быть исправленным. Плюс `updated_ts` и `claim_id` уезжают
в query-строку, а не в тело POST-запроса.

## 10. Точка `return` и статусы

Док, `PointType`: `source`, `destination`, `return` — и про `return` сказано «добавляется
автоматически и по умолчанию совпадает с точкой отправления». Провод (2026-08-12) показал ровно это:
на два отправленных пункта в ответе приходит три. **Сегодня это документировано** — справочник тут
лучше той HTML-справки, по которой мы писали спеку в июле 2025-го.

`ClaimStatus` в справочнике — 26 значений. У нас в документе 27: лишнее — `pay_waiting`, которого в
сегодняшнем справочнике нет. Ещё в нашем `TaxiClass` стоит `sdd_long` с комментарием «здесь быть не
должно».

## 11. Источник предупреждения — открытая строка у вендора и закрытый enum у нас

Док, сущность `ClaimWarningSourceApi`: **`Type: string`**, в описании перечислены прозой два значения
(`client_requirements`, `route_points`). Никакого `Enum` там нет.

Провод (2026-08-17): пришло третье — `taxi_requirements`, которого в описании нет и сегодня. Имя
совпадает с названием секции запроса (`taxi_requirements` — поле в теле `claims/create`), то есть
словарь растёт вместе со схемой запроса и закрытым не был никогда.

Наш документ моделировал это поле закрытым перечислением — и весь ответ `claims/info` перестал
декодироваться (TD-19). Ошибка наша, вендор её не обещал.

## 12. Доступ, тестовый контур, поддержка

Док, `quickstart.md`: логин и пароль «полученные от менеджера Яндекс Доставки»; токен берётся в
личном кабинете; «OAuth-токен действует неограниченное время». Дважды на странице — «В случае
проблем обратитесь к менеджеру Яндекс Доставки».

Хост в документации ровно один: `https://b2b.taxi.yandex.net`. Песочницы нет. Тестовый **кабинет**
существует, но только как срочный: в `errors.md` есть `required_tariffs_disabled_for_user` —
«Закончился срок тестового кабинета».

Вся страница «Технические детали интеграции» — одно предложение про TLS 1.2 и шифры PCI DSS v4.

Наш собственный ранний черновик (июль 2025, `YandexDostavka/Archive/express-delivery_corrected.yaml`)
объявлял второй сервер `https://b2b.taxi.tst.yandex.net/...` с описанием `Sandbox`. Ни в
документации, ни в Postman-коллекции реальных запросов такого хоста нет. Пример того, как документ,
который ты пишешь за вендора, обрастает выдумкой.

## 13. Ландшафт Habr и позиционирование

Метод: внутренний поисковый эндпоинт Habr (`/kek/v2/articles/?query=…&order=relevance&fl=ru&hl=ru`),
UI-поиск на странице результатов требует клика и через обычную загрузку страницы не отдаётся.
Запросы: «Яндекс Доставка API», «Яндекс Доставка API интеграция заявка», «B2B Cargo API Яндекс»,
«курьерская доставка API опыт подключения», «песочница sandbox тестовый контур API», «коды ошибок
API», «идемпотентность API», «версионирование API breaking changes», «OpenAPI спецификация генерация
клиента», «Diplodoc документация».

**Ниша свободна.** Про интеграцию с Express API Яндекс Доставки на Habr нет ничего: выдача по всем
формулировкам — новости о сервисе, маркетинговые кейсы и статьи про Яндекс.Карты. Ближайший
практический текст — бизнесовый, про подбор тарифов, а не про контракт.

Отобранные ссылки:

| Статья | Автор, дата | Роль в тексте |
|---|---|---|
| [Стажёр Вася и его истории об идемпотентности API](https://habr.com/ru/companies/yandex/articles/442762/) | jirfag, 2019-03-12, +213, **блог Яндекса** | Идемпотентность здесь работает ровно как обещано — и канонический русский текст про неё написал сам Яндекс |
| [Доставка день в день: алгоритмы поиска и назначения курьеров](https://habr.com/ru/companies/yandex/articles/887484/) | serkh, 2025-03-11, +29, блог Яндекса | Инженерия внутри сильная — тем заметнее качество контракта наружу |
| [Как мы снизили расходы на экспресс-доставку в Яндекс.Доставке на 13%](https://habr.com/ru/companies/gri/articles/924986/) | deliveryman (GRI), 2025-07-15 | Единственный практический текст про эту доставку — про тарифы, не про API |
| [Все тесты зелёные, платежи зависли: 5 граблей интеграции без песочницы](https://habr.com/ru/articles/1050584/) | vsinyavsky, 2026-06-23, +17 | Раздел про тестовый кабинет и отсутствие песочницы |
| [В аудит-логе Яндекс 360 ровно 26 типов событий](https://habr.com/ru/articles/1072594/) | zhogov1985, 2026-08-21 | Сосед по жанру: датированная сверка публичной документации вендора |
| [Возвращаем информативные ошибки API](https://habr.com/ru/companies/otus/articles/1018008/) | kmoseenk, 2026-04-02 | Опора для раздела про ошибки |
| [Как проектировать качественные API](https://habr.com/ru/companies/ruvds/articles/942916/) | interpres, 2025-09-03, +66 | Опора для «как бы я сделал» |
| [10 ошибок API](https://habr.com/ru/articles/1013924/) | rurikovich, 2026-03-26 | То же |
| [Documentation-Driven Development: генерируем Go-код из OpenAPI](https://habr.com/ru/companies/ru_mts/articles/1003562/) | cadeusept (МТС), 2026-02-26, +41 | Связка с прошлой статьёй; «как это выглядит, когда контракт первичен» |
| [Готов ли Swift OpenAPI Generator для продуктивного кода?](https://habr.com/ru/companies/ozontech/articles/769624/) | haron1020 (Ozon Tech), 2023-10-25 | Единственная Swift-статья про генератор; цитировалась в прошлой статье |
| [Яндекс.Карты API, я устал. Я ухожу](https://habr.com/ru/articles/534488/) | alPTR, 2020-12-22, +103 | Контрапункт по тону: текст, который я писать не хочу |

Не используем: [«Вредные советы от создателей API Яндекс.Карт»](https://habr.com/ru/companies/yandex/articles/221139/)
(2014) — при чтении оказалось, что это текст **самой команды Карт** о том, как не надо пользоваться их
API, а не критика вендора; для нашей рамки не подходит.

## 14. Сайт: polyglot был объявлен, но не работал — включён по-настоящему

Не про Яндекс, а про этот репозиторий. Обнаружено 2026-08-24 при подготовке публикации, исправлено там же.

`Gemfile` и `_config.yml` подключали **jekyll-polyglot**, а деплой шёл через
`actions/jekyll-build-pages@v1`, который запускает Jekyll в safe-режиме со списком разрешённых
плагинов GitHub Pages. Polyglot в этот список не входит и в продакшене **не загружался вовсе**.
Поэтому боевые адреса несли языковой суффикс в слаге
(`/articles/2025/06/03/openapi-source-of-truth.ru/`), раздела `/articles/en/` не существовало, а обе
перекрёстные ссылки «Read in English» / «Читать оригинал» вели в 404 — они были написаны в расчёте на
плагин, которого там нет.

**Что сделано.** Сборка переведена на `bundle exec jekyll build` (ruby/setup-ruby + bundler-cache),
так что объявленные плагины действительно загружаются. Каждому посту проставлен `permalink` без
языкового суффикса; polyglot сам разводит языки по деревьям:

| | русская | английская |
|---|---|---|
| прошлая статья | `/2025/06/03/openapi-source-of-truth/` | `/en/2025/06/03/openapi-source-of-truth/` |
| эта статья | `/2026/08/24/yandex-delivery-express-api/` | `/en/2026/08/24/yandex-delivery-express-api/` |

**Старые адреса сохранены**, потому что русская статья уже опубликована и на неё могли ссылаться:
`…-truth.ru/` отдаёт редирект через `jekyll-redirect-from`, а `…-truth.en/` — статическим
`index.html` с `meta refresh` в корне дерева. Статическим, а не плагинным, намеренно: этот путь должен
остаться в корневом дереве, а polyglot всё, что обрабатывает, растаскивает по языковым.

Проверено локальной сборкой: генерируются оба языковых дерева, оба редиректа ведут в правильные места,
все внутренние ссылки собранного сайта резолвятся, `notes/` в вывод не попадает.

Осталось: `_includes/lang-switcher.html` не подключён ни из одного шаблона — мёртвый код. Не трогал.

## 15. Что проверено экспериментом, а не по памяти

**Декодеры дат Foundation** (Swift 6.3.3, macOS 26.5) на тех самых строках, что приходят от Яндекса:

| Строка | `JSONDecoder.iso8601` | `ISO8601DateFormatter` | `…+.withFractionalSeconds` | `Date.ISO8601FormatStyle` |
|---|---|---|---|---|
| `2026-08-12T17:12:40.051944+00:00` | ok | **FAIL** | ok | ok |
| `2026-08-12T18:15:00+00:00` | ok | ok | **FAIL** | ok |
| `2026-08-12T17:12:40.051+00:00` | ok | **FAIL** | ok | ok |

Вывод, который и попал в статью: `ISO8601DateFormatter` нельзя настроить так, чтобы он принял обе
формы, а обе приходят в одном объекте. Попутно исправлено моё собственное заблуждение: современный
`JSONDecoder.dateDecodingStrategy = .iso8601` шестизначную дробную часть **принимает**.

**Координаты.** `number[]`, `Min items: 2`, `Max items: 2`, описание: «Координаты точек в виде массива
из двух вещественных чисел: долгота, широта — именно в таком порядке». В перечне кодов ошибок
`offers/calculate` есть `estimating.swapped_coordinates`.

**`point_id`.** Обязательное поле, в описании — «Целочисленный идентификатор точки (int64), уникальна в
рамках создания заявки», в примерах `1` и `2`.

**`cargo_loaders` — обычный `integer`, не `int64`**, и в справочнике Яндекса, и в нашем документе
(«Возможные значения: 0, 1, 2»). Предъявлять int64 к нему нельзя; в статье это оговорено отдельно,
чтобы претензия не выглядела шире, чем она есть.

**Типы в справочнике** размечены как `string` (283), `integer` (46), `boolean` (41), `number` (13),
`string[]` (8), `string<uuid>` (4), `number[]` (3); `int64` встречается только в прозе описаний.
